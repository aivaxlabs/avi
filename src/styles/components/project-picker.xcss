.project-picker-row {
  position: relative;
  z-index: 2;
  width: min(680px, 95%);
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--background-2);
  backdrop-filter: blur(4px);
  border: 1px solid var(--border-color);
  border-top: none;
  border-radius: 0 0 16px 16px;
  padding: 0 10px;
  gap: 12px;

  .project-picker-holder {
    position: relative;
    min-width: 0;
  }

  .project-picker {
    min-width: 0;
    max-width: min(420px, 70vw);
    height: 28px;
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 0 8px;
    color: var(--text-3);
    border-radius: 7px;
    font-size: 11px;
    background: var(--background-2);
    transition: color 180ms var(--base-transition),
      background 180ms var(--base-transition),
      border-color 180ms var(--base-transition),
      transform 180ms var(--base-transition);

    @media (prefers-reduced-motion: reduce) {
      transition: none;
    }

    &:hover:not(:disabled) {
      color: var(--text-1);
      background: var(--item-hover);
      border-color: var(--border-color);
    }

    &:active:not(:disabled) {
      transform: scale(.98);
    }

    &:focus-visible {
      outline: 2px solid var(--text-3);
      outline-offset: 2px;
    }

    &:disabled {
      color: var(--text-3);
      background: transparent;
      cursor: not-allowed;
      opacity: .72;
    }
  }

  .project-picker-menu {
    position: absolute;
    left: 0;
    bottom: calc(100% + 6px);
    z-index: 130;
    width: 280px;
    padding: 6px;
    background: var(--background-1);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    box-shadow: 0 16px 40px rgb(0 0 0 / 22%);
  }

  .project-picker-search {
    display: grid;
    grid-template-columns: 16px minmax(0, 1fr);
    align-items: center;
    gap: 6px;
    padding: 0 6px;
    color: var(--text-3);

    input {
      min-width: 0;
      height: 30px;
      padding: 6px 0 !important;
      color: var(--text-1);
      background: transparent;
      border: 0;
      border-radius: 0;
      outline: 0;
      font-size: 12px !important;
    }
  }

  .project-picker-list {
    max-height: 224px;
    overflow-y: auto;
  }

  .project-picker-list button,
  .project-picker-action {
    width: 100%;
    min-width: 0;
    min-height: 30px;
    display: grid;
    grid-template-columns: 16px minmax(0, 1fr) auto 16px;
    align-items: center;
    gap: 7px;
    padding: 6px 8px;
    color: var(--text-1);
    background: transparent;
    text-align: left;
    font-size: 12px;
  }

  .project-picker-list button:hover,
  .project-picker-action:hover {
    background: var(--item-hover);
  }

  .project-picker-list button.active,
  .project-picker-action.active {
    background: var(--item-active);
  }

  .project-picker-list button:focus-visible,
  .project-picker-action:focus-visible {
    outline: 2px solid var(--text-3);
    outline-offset: -2px;
  }

  .project-picker-list button > span,
  .project-picker-list button > small {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .project-picker-list button > small {
    max-width: 82px;
    color: var(--text-3);
    font-size: 10px;
  }

  .project-picker-list button > svg:last-child {
    grid-column: 4;
    color: var(--text-1);
  }

  .project-picker-empty {
    display: block;
    padding: 10px 8px;
    color: var(--text-3);
    font-size: 11px;
  }

  .project-picker-divider {
    height: 1px;
    margin: 5px 6px;
    background: var(--background-3);
  }

  .project-picker-action {
    grid-template-columns: 16px minmax(0, 1fr) 16px;
  }

  .project-picker-action svg,
  .project-picker-list button > svg:first-child {
    color: var(--text-3);
  }

  .project-picker-action > svg:last-child {
    color: var(--text-1);
  }

  .context-usage {
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 2px 0;
    color: var(--text-3);
    font-size: 11px;
    font-variant-numeric: tabular-nums;
  }

  .context-usage-ring {
    --context-progress: 0deg;
    width: 16px;
    height: 16px;
    position: relative;
    display: block;
    border-radius: 50%;
    background: conic-gradient(var(--text-1) var(--context-progress),
        var(--background-4) 0);

    &::after {
      content: '';
      position: absolute;
      inset: 3px;
      background: var(--background-1);
      border-radius: 50%;
    }
  }

  .project-picker-path,
  .project-picker-branch span {
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .project-picker-path {
    flex: 1 1 auto;
  }

  .project-picker-branch {
    min-width: 0;
    max-width: 160px;
    display: flex;
    align-items: center;
    gap: 4px;
    padding-left: 7px;
    border-left: 1px solid var(--border-color);
  }

  .project-picker-branch svg,
  .project-picker-lock {
    flex: 0 0 auto;
  }

  .project-picker-spinner {
    animation: project-picker-spin 800ms linear infinite;

    @media (prefers-reduced-motion: reduce) {
      animation: none;
    }
  }
}

@keyframes project-picker-spin {
  to {
    transform: rotate(360deg);
  }
}
